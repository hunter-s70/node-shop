<%- include('../includes/head.ejs') %>
</head>

<body>
<%- include('../includes/navigation.ejs') %>
<main>
    <% if (products.length) { %>
        <ul>
            <% products.forEach(item => { %>
                <li>
                    <p><% item.productData.title %> (<% item.qty %>)</p>
                    <a href="/delete-cart-item/<%= item.productData.id %>"
                       class="btn">Delete</a>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <h1>No products in Cart!</h1>
    <% } %>
</main>
<%- include('../includes/end.ejs') %>